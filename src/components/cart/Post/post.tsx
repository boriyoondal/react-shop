import React from "react";
import { Product } from "src/@types/types";

type Type = {
    products : Product
    loading : boolean
}
export default function Post({products, loading} : Type) {
    
    return (
        <div>

        </div>
    )
}