import React from "react";
import styled from "@emotion/styled"
import { css } from "@emotion/react"

const Page = ({total, limit, page, setPage } : any) => {
  

  return (
    <div>
      <nav>
        
        
      </nav>
    </div>
  );
}

export default Page;

